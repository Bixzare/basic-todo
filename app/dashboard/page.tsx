import React from 'react';
import { ClipboardCheck } from 'lucide-react';
export default function Page(){
    return(
      <div className = "w-full h-full bg-blue-50 p-2">Home page of the dashboard
                      <ClipboardCheck className = "size-10"/>
                      </div>
    )
}